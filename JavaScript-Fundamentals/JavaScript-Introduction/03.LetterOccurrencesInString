function letterOccureInString(word, letter){
  let times = 0;
  for (let i=0; i<=word.length; i++){
    if(word[i] == letter)
      times++;
  }
  console.log(times);
}
letterOccureInString ('hello', 'l');
